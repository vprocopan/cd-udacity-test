version: 2.1

jobs:
  create_infrastructure:
    docker:
      - image: amazon/aws-cli
    steps:
      - checkout
      - run:
          name: Ensure backend infrastructure exist
          command: |
            aws cloudformation deploy --template-file template.yml --stack-name my-stack --region eu-central-1 --aws_access_key_id AKIASE6LLLL2WGLII52I --aws_secret_access_key 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
workflows:
  my_workflow:
    jobs:
      - create_infrastructure